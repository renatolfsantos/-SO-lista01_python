# Declarando variáveis
x:float = 0
y:float = 0
diferença:float = 0

# Início
x = float(input("Digite o primeiro número: "))
y = float(input("Digite o segundo número: "))

diferença = x - y

print(f"A diferença entre eles é de aproximadamente: {diferença:.2f}")
# Fim